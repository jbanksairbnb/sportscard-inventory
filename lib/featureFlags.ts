// Feature flags. Set to control gates that should be easy to toggle without
// hunting through the codebase. Each flag should be documented in place.
//
// (Currently empty — REQUIRE_APPLICATION was removed when buying access
// became immediate and selling became the only gated privilege. See
// can_sell on user_profiles + lib/sellerGuard.ts.)

export {};
